onetimevarforstoppingshit = false // this is so that when the collsion box is touchingground it does not keep setting timer
parentalFigure = object_get_parent(obj_testForDaplayer)


// Make it so that this object can change the normalState variable